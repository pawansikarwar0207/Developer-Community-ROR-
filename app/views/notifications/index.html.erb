<div class="container col-lg-10 mx-auto mt-3">
  <% if @notifications.present? %>
    <h2>Notifications</h2>
    <ul class="list-group" id="notifications">
      <%= render @notifications %>
    </ul>
  <% else %>
  <h3>No notification is present!</h3>
  <% end %>
</div>
